# Thin Unraid integration

This directory is packaged into the `.txz` installed by the `.plg`. It contains
only the webGUI launcher and status integration. Infrastructure credentials and
route mutations belong exclusively to the Route Manager container.

